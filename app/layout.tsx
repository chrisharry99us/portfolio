import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
});

export const metadata: Metadata = {
  title: "Chris Rodrigues | Financial Data Scientist",
  description:
    "Software engineer turned data scientist. I build tools that make financial data actionable.",
  openGraph: {
    title: "Chris Rodrigues | Financial Data Scientist",
    description:
      "Explore my projects in financial analytics, ML modeling, and data pipelines.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}